//import actions
import {  } from "../actions/index"
//estado global redux
const initialState = {
    
};

export default function rootReducer(state, action) {
  switch (action.type) {


    //Default
    default:
      return state;
  }
}
